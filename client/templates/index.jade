#index.container-fluid
  .well
    h2 操作說明
    ol
      li 在右下角裁切照片，並保持人像在右方紅色所示 facebook cover 範圍內
      li 於左下角填寫基本資料
      li 存檔後寄給工作人員!

  .row
    .col-xs-12
      #preview-container
        #preview
          #content-text
          #content-header
            img.img-responsive(src="/index/ioh_brand.png", alt="")
            .text-right
              span(ng-bind="page.name")
              br
              span(ng-bind="page.exprs[0]")
              br
              span(ng-bind="page.exprs[1]")
              br
              span(ng-bind="page.exprs[2]")
  .row
    .col-xs-6
      form(role="form")
        .form-group
          p 大頭照
          .btn-group
            button.btn.btn-default(type="button") 不使用
            button.btn.btn-default(type="button") 使用
        .form-group
          p 地點文字顏色
          .btn-group
            button.btn.btn-default(type="button") 白色
            button.btn.btn-default(type="button") 黑色
        .form-group
          label 姓名
          input.form-control(type="text" ng-model="page.name")
        .form-group
          label 經歷1 (紅字)
          input.form-control(type="text" ng-model="page.exprs[0]")
        .form-group
          label 經歷2
          input.form-control(type="text" ng-model="page.exprs[1]")
        .form-group
          label 經歷3
          input.form-control(type="text" ng-model="page.exprs[2]")
        .form-group
          label 地點
          input.form-control(type="text" placeholder="ex MIT Media Lab")
        .form-group
          label 簡介
          textarea.form-control#forbrief( rows="3")
